此仓库为BG6TOE及关联呼号签发的所有QSL卡片的数字签名信息。

该仓库可用为

- 证明某一QSL签名产生于某一时间点以前
- 证明某一QSL签名是基于BG6TOE的意志产生的

此仓库仅包含签名的哈希，不包含原始签名及QSL信息。

格式

- raw_index: 批量上传的签名索引
- sig_index: 签名哈希的索引
- raw_index的文件名为签名索引的SHA256哈希的16进制编码
  - 文件名的前两位抽出作为目录名（亦包含在文件名中）
- sig_index的文件名为签名索引的SHA256哈希的32进制编码
  - 文件名前两位单独抽出作为目录名（不包含在内层目录中）
  - 文件夹内为指向原始上传数据的符号连接
